language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t pumba94/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true pumba94/docker-react npm run test